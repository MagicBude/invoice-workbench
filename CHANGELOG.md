# 版本变更记录

## 未发布：v0.1 UI、性能与发布收尾

- 调整状态统计卡片响应式断点，桌面端更早进入 7 列紧凑布局。
- 有解析结果后自动缩小 PDF 上传区域，把更多页面空间留给工作台。
- “显示与导出字段”改为可展开 / 收起，并默认收起，减少筛选区到结果表格之间的垂直距离。
- 字段设置展开状态保存在当前浏览器。
- 缩小工作台模块之间的垂直间距和导出操作栏高度。
- 批量导入结果改为每 8 条刷新一次，批次完成后统一执行重复检测，降低大批量文件下的重复渲染和扫描开销。
- 新增大批量性能检查文档和 v0.1.0 发布检查清单。

## 未发布：批量筛选与高效复核工作流

- 状态统计卡片改为可点击筛选入口，支持全部、自动成功、待复核、人工已确认、失败、重复和金额异常。
- 增加文件名、发票号、购销方、项目名称等字段的本地搜索。
- 增加文件日期、开票日期、价税合计和待处理优先排序。
- 新增独立的人工复核状态，人工确认不会覆盖解析器原始判断。
- 已确认记录再次编辑后自动恢复为未确认，避免确认状态与最新数据不一致。
- 单张复核面板增加“上一待处理 / 下一待处理”。
- 支持导出全部记录或当前筛选、搜索、排序后的结果。
- 新增人工复核字段的中文显示与导出。
- 增加批量视图筛选、搜索、排序和人工确认相关测试。

## 未发布修正：按页面视觉顺序解析 PDF 文本

- 修复 PDF.js 直接按内容流顺序拼接文本导致的双栏字段错位。
- 根据文本对象 X/Y 坐标恢复“从上到下、同一行从左到右”的视觉顺序。
- 保留明显列间距，便于区分项目名称、规格型号、数量、金额等明细列。
- 修复购买方 / 销售方名称、税号错位或重复的问题。
- 修复测试中“无效税号标签占用槽位”导致的税号错配。
- 项目名称改为优先读取明细第一列，避免吞入规格型号或整段页面文本。
- 增加视觉顺序、列边界、双栏购销方和多列商品明细的回归测试。

## 未发布修正：购销方税号与项目名称解析

- 修复购买方 / 销售方税号被拼接到名称字段的问题。
- 税号标签缺失时，可从名称尾部识别并拆分 15～20 位税号。
- 兼容“统一社会信用代码 / 纳税人识别号”被 PDF.js 拆空格或省略冒号的文本层。
- 重写项目名称表头判断，避免把“规格型号 / 单位 / 数量 / 单价 / 金额”等表头识别为项目名称。
- 支持从标准明细表头后的第一条“*分类*项目”结构提取项目名称。
- 增加上述真实场景的匿名化回归测试。

### 发票字段解析增强

- 增强发票号码与开票日期识别，兼容 8 位号码、20 位号码和紧凑日期。
- 增加购买方 / 销售方名称和税号解析，兼容顺序布局与双栏文本顺序。
- 重构金额提取为“明确标签 → 合计行 → 数学关系 → 启发式兜底”。
- 支持红字负数金额、多税率、免税 / 不征税和项目名称基础提取。
- 增加缺失关键字段的校验消息与更完整的 invoice-core 单元测试。


## 未发布

### 已加入

- PDF 多文件点击选择与拖拽导入
- 非 PDF 文件过滤与中文提示
- 逐文件处理进度和批次结果统计
- PDF.js 浏览器本地文本层质量判断
- 加密、损坏等 PDF 读取错误分类
- 批量处理中的 PDF.js 文档资源主动释放
- File API、PDF.js 与顺序批处理相关中文学习型注释

## 0.1.0 - 2026-08-14

建立项目第一版仓库基础结构。

### 已加入

- 本地优先（Local-first）的产品与架构说明
- Next.js 静态 Web 应用基础框架
- GitHub Pages 自动部署工作流
- PDF.js 浏览器文本提取入口
- `invoice-core` 发票解析核心包
- 统一导出字段注册表
- 基础发票号码、开票日期与金额解析
- 金额关系校验
- 发票号码重复检测
- 可编辑结果表格
- 用户可自定义选择 Excel / CSV 导出列
- 面向 Codex / AI Agent 的 `AGENTS.md` 开发约束
- 分类整理的产品、架构、开发、部署、路线图与架构决策文档

### 结果复核与自定义导出体验增强

- 结果表格增加“显示更多字段 / 收起详细字段”，可直接查看和修改发票类型、购销方、税率与项目名称。
- 导出列增加“全选”和“恢复默认”，并保证至少保留一个导出字段。
- localStorage 中的旧版或异常字段偏好会自动清理并按统一字段顺序恢复。
- Excel / CSV 中的解析状态、重复状态和金额校验统一导出为中文。
- 置信度导出为百分比形式。
- Excel / CSV 增加金额合计行，金额继续保持数值类型以便后续计算。
- 增加导出数据和字段偏好相关自动化测试。
