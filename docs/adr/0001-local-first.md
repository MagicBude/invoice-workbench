# ADR-0001：默认采用本地优先

- 状态：已接受
- 日期：2026-08-14

## 背景

发票包含公司名、税号、金额等敏感数据，同时多数标准电子发票 PDF 已包含文本层。

## 决策

v0.1 默认在浏览器内完成 PDF 文本读取、字段解析、校验与导出，不上传原始 PDF。

## 影响

优点：

- 隐私更好
- 无服务器基础成本
- 部署简单
- 标准电子发票处理速度快

代价：

- 扫描件暂不能依靠同一路径处理
- 大批量性能受浏览器能力限制
- 未来 OCR 仍需要后端服务或本地模型
