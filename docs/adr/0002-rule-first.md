# ADR-0002：规则优先，AI 作为补充

- 状态：已接受
- 日期：2026-08-14

## 背景

发票属于高度结构化文档，很多关键字段具有稳定标签和格式。

## 决策

解析优先使用确定性规则，AI 不作为默认主路径。

优先级：

```text
规则 → 上下文 / 位置 → 启发式 → OCR → AI → 人工复核
```

## 影响

- 成本更低
- 结果更可解释
- 更容易编写测试
- 需要持续维护不同发票版式规则
