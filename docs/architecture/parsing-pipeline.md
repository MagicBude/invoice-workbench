# 发票解析流水线

## v0.1 流程

```text
PDF 文件
   ↓
校验文件类型
   ↓
PDF.js getTextContent()
   ↓
normalizeInvoiceText()
   ↓
parseFilenameMetadata()
   ↓
detectInvoiceType()
   ↓
字段提取器
   ├── 发票号码
   ├── 开票日期
   ├── 销售方 / 购买方
   ├── 金额
   ├── 税率
   └── 项目名称
   ↓
validateAmountRelation()
   ↓
重复检测
   ↓
置信度与解析状态
   ↓
InvoiceRecord
```

## 1. PDF 文本层优先

PDF.js 使用 `getTextContent()` 获取每页文本。

这是标准电子发票的主处理路径。

如果提取结果几乎为空，应判断为：

```text
NO_TEXT_LAYER
```

而不是继续用大量正则强行猜测。

## 2. 文本标准化

建议统一处理：

- 全角数字 → 半角数字
- 特殊“月 / 日”Unicode 变体
- 连续空格
- 不同冒号形式
- 金额千位分隔符
- 常见人民币符号


## 3. 购销方区块识别

PDF 文本层中的购买方和销售方常见两种顺序：

```text
顺序布局：购买方标题 → 购买方字段 → 销售方标题 → 销售方字段
双栏布局：购买方标题 + 销售方标题 → 两组名称 → 两组税号
```

解析器应先判断标题与字段标签的相对顺序，再选择对应提取策略。
不能简单用“第一个名称就是购买方”覆盖所有发票版式。

## 4. 金额解析层级

不要长期依赖“最大金额就是价税合计、最小金额就是税额”这样的简单规则。

推荐优先级：

### 第一级：明确标签上下文

例如：

- 合计
- 税额
- 价税合计（小写）

### 第二级：位置与邻近文本

通过 PDF 文本项坐标增强字段与金额之间的关联。

### 第三级：启发式兜底

例如最大金额 / 最小金额，只在没有更可靠信息时使用。

### 第四级：数学验证

```text
不含税金额 + 税额 ≈ 价税合计
```

误差建议 `<= 0.01`。

## 5. 文件名作为第二信息源

文件名可能包含：

- 日期
- 业务简称
- 金额

例如：

```text
2026.08.14-京东-128.50元.pdf
```

这些数据不应替代发票正文，但可以用于：

- 补充业务字段
- 金额交叉校验
- 后续用户自定义规则

## 6. 解析置信度

初始版本可以基于简单规则：

- 找到发票号：+0.25
- 找到开票日期：+0.15
- 找到三项金额：+0.30
- 金额校验通过：+0.20
- 找到销售方：+0.10

后续应根据真实发票样本逐步调整。

## 7. 低置信度处理

系统不能把低置信度结果伪装成成功。

低置信度结果应：

- `parseStatus = review`
- 高亮缺失字段
- 提供人工编辑
- P1 阶段提供 PDF 与字段并排复核

## 8. 风险信号与人工复核

以下情况即使其他字段较完整，也应进入 `review`：

- 金额只能依赖最大值 / 最小值等启发式规则推断。
- 文件名中的预期金额与发票正文价税合计不一致。
- 金额关系校验失败。

启发式金额即使数学关系通过，也不能因此自动升级为“识别成功”，因为数学关系可能来自同一组推断值。
